/**
 * Terminal Types
 * Re-exports from central types for backward compatibility
 */

export type {
    TerminalLine,
    CommandBlock,
    TuiSelector,
    ThemeId,
    FontId,
    ThemeColors,
} from "@/types";
