# Environment Variables
# Copy this to .env.local and add your values

# ===========================================
# REQUIRED FOR DEPLOYMENT
# ===========================================

# Your GitHub username
GITHUB_USERNAME=qtremors

# Secret command to access admin mode in terminal
# Choose something unique that won't be typed accidentally
# Password is set on first use
ADMIN_SECRET=your_secret_command

# Database URL (NeonDB on Vercel)
# Vercel automatically sets these when you add Neon integration
DATABASE_URL=
DIRECT_URL=

# ===========================================
# OPTIONAL
# ===========================================

# GitHub Personal Access Token (recommended to avoid rate limits)
# Create at: https://github.com/settings/tokens
# Scopes: public_repo (read-only)
GITHUB_TOKEN=

# Authentication signing secret (auto-generated if not set)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=

# Google Gemini API key (for AI newspaper content)
# Get at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# ===========================================
# VERCEL + NEONDB DEPLOYMENT
# ===========================================
# 1. Connect project to GitHub in Vercel
# 2. Add Neon Postgres from Vercel Storage tab
#    - This auto-adds DATABASE_URL and DIRECT_URL
# 3. Set remaining environment variables above
# 4. Deploy - tables created automatically via prisma db push
# 5. After first deploy, seed newspapers via Vercel CLI:
#    vercel env pull .env.local
#    npx tsx prisma/seed-newspaper.ts
