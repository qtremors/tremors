# Local Environment Variables
# Copy this to .env.local and add your values

# GitHub Personal Access Token (optional but recommended to avoid rate limits)
# Create one at: https://github.com/settings/tokens
# Scopes needed: public_repo (read-only)
GITHUB_TOKEN=

# Your GitHub username
GITHUB_USERNAME=qtremors

# Secret command to access admin mode in terminal
# Choose something unique that won't be typed accidentally
# This is the ONLY thing you need for admin access - password is set on first use
ADMIN_SECRET=

# Authentication signing secret (optional - auto-generated if not set)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=

# Database URL (SQLite for local, Postgres for Vercel)
DATABASE_URL="file:./dev.db"
